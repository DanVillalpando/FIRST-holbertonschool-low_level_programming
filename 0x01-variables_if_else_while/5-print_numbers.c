#include <stdio.h>
/*Only one header*/

int main/*Main of the function*/(void)
{
int numb = '0';
for (numb <= '9')
{
printf(numb);
numb++;
}
printf(numb);
/*Just prints numbers 0 to 9*/
return (0);
/*Return zero*/
}
