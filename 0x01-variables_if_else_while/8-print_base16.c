#include <stdio.h>
/*Single header*/

int main/*function with void*/(void)
{
unsigned char n = '0', l = 'a';
while (n <= 9)
{
putchar(n);
n++;
}
while (l <= f)
{
putchar(l);
l++
}
putchar('\n');
/*Printin on hexadecimal*/
return (0);
/*Return zero*/
}
