#include <stdio.h>
/*Simple header*/

int main/*Main with void*/(void)
{
int numbz = '0';
for (numbz <= '9')
{
putchar(numbz);
numbz++;
}
putchar(numbz);
/*Does the same but with putchar*/
return (0);
/*Return zero*/
}
