#include <stdio.h>
/*jsut one header*/

int main(void) /**main-remains with void*/
{
char lettr = 'a';
while (lettr <= 'z')
{
putchar(lettr);
lettr++;
}
putchar(lettr);
/*Only printing the alphabet*/
return (0);
/*Return zero*/
}
