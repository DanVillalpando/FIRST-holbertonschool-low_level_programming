#include <stdarg.h>
#include <stdio.h>
#include <stdlib.h>

void print_all(const char * const format, ...)
{
  int i = 0;
  va_list l;

  va-start(l, format);

  while(format != NULL)
    {
      switch
    case c;
      
    }
}
