#include <stdio.h>
#include <ctype.h>
#include "main.h"

int _islower(int c);
int main(void)
{
  char ex;
    if ( isalpha (ex) )
          {
            putchar('1');
          }
        else
            putchar('0');

    return (0);
}
