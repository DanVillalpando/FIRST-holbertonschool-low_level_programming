#include <stdio.h>
#include <ctype.h>
#include "main.h"
#include <math.h>

int print_sign(int n);
int main(void)
{
  float fabs;
    if ( fabs (x) == + )
          {
            putchar('+');
	    return (1);
          }
    if else ( fabs (x) == - )
	      {
	      putchar('-');
    return ('0');
	      }
    else ( fabs (x) < 0 )
	   putchar('-1');
    
    return (0);
}
