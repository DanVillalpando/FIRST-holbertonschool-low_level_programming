#include <stdio.h>
#include "main.h"

void print_alphabet(void);
int main(void)
{
  char alph[26] = "abcdefghijklmnopqrstuvwxyz";
  for(int i = 0; i < 26; i++)
    {
      putchar(alph[i]);
    }
      putchar('\n');
      return (void);
}
