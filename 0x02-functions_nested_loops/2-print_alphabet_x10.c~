#include <stdio.h>
#include "main.h"

void print_alphabet_x10(void);
int main(void)
{
  char alph[26] = "abcdefghijklmnopqrstuvwxyz";
  int j = "0";
    while (j<10)
      {
	for (int i = 0; i < 8; i++)
	  {
	    putchar(alph[26]);
	  }
	putchar('\n');
	j++;
      }
    return (0);
}
  
